=== Python Iterators ===
Konu: iterators
Kaynak: https://www.w3schools.com/python/python_iterators.asp

📖 AÇIKLAMALAR:
1. An iterator is an object that contains a countable number of values.
2. An iterator is an object that can be iterated upon, meaning that you can 
traverse through all the values.
3. Technically, in Python, an iterator is an object which implements the 
iterator protocol, which consist of the methods __iter__() 
and __next__().
4. Lists, tuples, dictionaries, and sets are all iterable objects. They are iterable
containers which you can get an iterator from.
5. All these objects have a iter() method which is used to get an iterator:

💻 KOD ÖRNEKLERİ:

Örnek 1:
```python
mytuple = ("apple", "banana", "cherry")myit = iter(mytuple)
    print(next(myit))print(next(myit))print(next(myit))
```

Örnek 2:
```python
mystr = "banana"myit = iter(mystr)
    print(next(myit))print(next(myit))print(next(myit))
    print(next(myit))print(next(myit))print(next(myit))
```

Örnek 3:
```python
mytuple = ("apple", "banana", "cherry")
for x in mytuple:
      print(x)
```

Örnek 4:
```python
mystr = "banana"
for x in mystr:
      print(x)
```

Örnek 5:
```python
class MyNumbers:  def __iter__(self):    self.a = 
  1    return self  def __next__(self):    
  x = self.a    self.a += 1    return x
myclass = MyNumbers()myiter =
   iter(myclass)print(next(myiter))
  print(next(myiter))print(next(myiter))print(next(myiter))
  print(next(myiter))
```

Örnek 6:
```python
class MyNumbers:  def __iter__(self):    self.a = 
  1    return self  def __next__(self):    
  if self.a <= 20:      x = self.a      
  self.a += 1      return x    
  else:      raise StopIterationmyclass = 
  MyNumbers()myiter =
   iter(myclass)for x in myiter:  
  print(x)
```

================================================================================

